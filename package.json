{
  "name": "gamelib-root",
  "version": "1.0.0",
  "description": "Root package to manage GameLib client and server",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "start": "npx concurrently \"npm run start --prefix client\" \"npm run start --prefix server\"",
    "install-all": "npm install --prefix client --legacy-peer-deps && npm install --prefix server",
    "prisma:generate": "npm run prisma:generate --prefix server",
    "prisma:migrate": "npm run prisma:migrate --prefix server",
    "prisma:studio": "npm run prisma:studio --prefix server",
    "prisma:reset": "npm run prisma:reset --prefix server",
    "db:up": "docker compose up db -d",
    "db:down": "docker compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
